/*#include <stdio.h>

int main() {
	int N, M;
	scanf("%d %d", &N, &M);

	
}*/

/*
void push(int data) {
	tail = (tail + 1) % n;

	if (head == tail) {
		if (Q[tail] == NULL) {
			Q[tail] = data;
			printf("overflow\n");
			return;
		}
		else {
			printf("overflow\n");
			return;
		}
	}
	Q[tail] = data;
}

void pop() {


	if (head == tail && Q[head] == NULL) {
		printf("underflow\n");
		return;
	}

	head = (head + 1) % n;
	Q[head] = NULL;
}*/