#include <stdio.h>

/*int main() {

	int n;
	scanf_s("%d", &n);

	int arr[1000];
	int max = 0;
	for (int i = 0; i < n; i++) {
		scanf_s("%d", &arr[i]);
		if (arr[i] > max)
			max = arr[i];
	}

	double sum = 0;
	for (int j = 0; j < n; j++) {
		sum = sum + ((double)arr[j] / max) * 100;
	}

	printf("%f \n", (double)sum / n);

	return 0;

}*/